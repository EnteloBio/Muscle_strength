# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Muscle_strength") is a new project for a "Muscle grip strength game." As of the initial commit, the repo contains only a `README.md` — there is no source code, build system, test framework, or dependency manifest.

### Current state
- **No application code** exists yet.
- **No dependencies** to install (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.).
- **No lint, test, or build commands** are configured.
- **No services** to start.

### For future agents
- Once code is added, update the VM environment setup script to install the relevant dependencies (e.g., `npm install`, `pip install -r requirements.txt`).
- Check `README.md` for any updated setup instructions as the project evolves.
